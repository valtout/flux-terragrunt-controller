apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: terragruntstacks.terragrunt.run
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
spec:
  group: terragrunt.run
  names:
    kind: TerragruntStack
    listKind: TerragruntStackList
    plural: terragruntstacks
    shortNames:
      - tgstack
    singular: terragruntstack
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      schema:
        openAPIV3Schema:
          type: object
          properties:
            apiVersion:
              type: string
            kind:
              type: string
            metadata:
              type: object
            spec:
              type: object
              required:
                - filter
                - branch
              properties:
                filter:
                  type: string
                  description: "Path within the GitRepository to monitor for changes"
                branch:
                  type: string
                  description: "Git branch to monitor"
            status:
              type: object
              properties:
                lastHandledReconcileAt:
                  type: string
                  description: "Last reconciliation timestamp"
                lastCommitSHA:
                  type: string
                  description: "SHA of the last detected change"